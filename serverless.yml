service: typescript-serverless-boilerplate

plugins:
  - serverless-plugin-common-excludes
  - serverless-plugin-include-dependencies

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x
  stage: ${opt:stage, 'dev'}
  region: us-east-1
  apiGateway:
    restApiId: ${cf:api-${self:provider.stage}.ApiGatewayRestApiId}
    restApiRootResourceId: ${cf:api-${self:provider.stage}.ApiGatewayRestApiResourceId}

package:
  individually: true
  patterns:
    - '!node_modules/**'
    - '!src/**'
    - '!scripts/**'
    - '!dist/**'
    - '!layers/**'
    - '!schema.prisma'
    - '!tsconfig.json'
    - '!tslint.json'

functions:
  hello:
    handler: dist/endpoints/hello/handler.hello
    events:
      - http:
          path: /hello/world
          method: get
