{
  "name": "typescript-serverless-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --build && sh scripts/moveFiles.sh",
    "deploy:prod": "yarn build && sls deploy -s prod",
    "deploy": "yarn build && sls deploy -s dev",
    "deploy:function": "yarn build && sls deploy -s dev function",
    "deploy:prod:function": "yarn build && sls deploy -s prod function"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.104",
    "serverless-plugin-common-excludes": "^4.0.0",
    "serverless-plugin-include-dependencies": "^5.0.0",
    "tsc": "^2.0.4",
    "typescript": "^4.8.3"
  }
}
